// let numOfSegments = 12;
let numOfSegments = 360;
let radius = 100;

function setup() {
    createCanvas(500, 500);
    background(0);

    colorMode(HSB, 360, 100, 100);
    angleMode(DEGREES);
    noStroke();
}

function draw() {

}